# eclipse
bin
*.launch
.settings
.metadata
.classpath
.project

# idea
out
*.ipr
*.iws
*.iml
.idea

# gradle
build
.gradle

# other
eclipse
run
run-data
logs
CLAUDE.md
# console capture from driving :forge:runGameTestServer (see .github/workflows/gradle-build.yml)
gametest.log

# Files from Forge MDK
forge*changelog.txt

# datagen output (regenerated by :forge:runData)
/forge/src/main/generated/
/*/src/generated/
# leftover from the pre-multiloader single-module ForgeGradle layout
/src/

# architectury-transformer debug log
.architectury-transformer/

# locally-sourced compileOnly/testOnly jars (e.g. forge/libs/create-local-test) - not build output,
# but not source either, and too large/binary to belong in the repo
/*/libs/

# live progress dashboard todo-list state written by the bump-mc-version skill (run artifact, not source)
/.claude/skills/bump-mc-version/progress.json
/.claude/skills/bump-mc-version/.venv/
/.claude/skills/bump-mc-version/__pycache__/
