# Sets default memory used for gradle commands. Can be overridden by user or command line properties.
# This is required to provide enough memory for the Minecraft decompilation process.
org.gradle.jvmargs=-Xmx8G
org.gradle.daemon=true


## Environment Properties

# The Minecraft version must agree with the Forge version to get a valid artifact
# Targeting 1.20.4, not 1.20.3 - 1.20.3 was a ~1-week bridge release the mod ecosystem largely
# skipped (Fabric's own announcement asked people not to pressure modders into supporting it):
# Architectury API never published a 1.20.3 build at all (its ranges jump straight from
# "1.20-1.20.2" to "1.20.4-1.20.6"), and NeoForge only ever shipped 20.3.x betas, never a stable
# release. 1.20.4 has full stable coverage across Forge, Architectury API, Fabric, NeoForge, and
# GeckoLib. Confirmed with the user before deviating from the requested 1.20.3 target.
minecraft_version=1.20.4
# The Minecraft version range can use any release version of Minecraft as bounds.
# Snapshots, pre-releases, and release candidates are not guaranteed to sort properly
# as they do not follow standard versioning conventions.
# Tightened to just 1.20.4 (see 1.20.2's note above for why a wide range is risky).
minecraft_version_range=[1.20.4,1.20.5)
# The Forge version must agree with the Minecraft version to get a valid artifact
# 49.2.0 is Forge's own "recommended" build for 1.20.4 (files.minecraftforge.net) - the newest
# build is 49.2.9, but recommended is the vetted/stable one, matching this repo's prior pins.
forge_version=49.2.0
# The Forge version range can use any version of Forge as bounds or match the loader version range
forge_version_range=[49,)
# The loader version range can only use the major version of Forge/FML as bounds
loader_version_range=[49,)
# The mapping channel to use for mappings.
# The default set of supported mapping channels are ["official", "snapshot", "snapshot_nodoc", "stable", "stable_nodoc"].
# Additional mapping channels can be registered through the "channelProviders" extension in a Gradle plugin.
#
# | Channel   | Version              |                                                                                |
# |-----------|----------------------|--------------------------------------------------------------------------------|
# | official  | MCVersion            | Official field/method names from Mojang mapping files                          |
# | parchment | YYYY.MM.DD-MCVersion | Open community-sourced parameter names and javadocs layered on top of official |
#
# You must be aware of the Mojang license when using the 'official' or 'parchment' mappings.
# See more information here: https://github.com/MinecraftForge/MCPConfig/blob/master/Mojang.md
#
# Parchment is an unofficial project maintained by ParchmentMC, separate from Minecraft Forge.
# Additional setup is needed to use their mappings, see https://parchmentmc.org/docs/getting-started
mapping_channel=official
# The mapping version to query from the mapping channel.
# This must match the format required by the mapping channel.
mapping_version=1.20.4


## Mod Properties

# The unique mod identifier for the mod. Must be lowercase in English locale. Must fit the regex [a-z][a-z0-9_]{1,63}
# Must match the String constant located in the main mod class annotated with @Mod.
mod_id=extrabiomes
# The human-readable display name for the mod.
mod_name=ExtraBiomes
# The license of the mod. Review your options at https://choosealicense.com/. All Rights Reserved is the default.
mod_license=MIT
# The mod version. See https://semver.org/
mod_version=3.10.0-beta-5
# The group ID for the mod. It is only important when publishing as an artifact to a Maven repository.
# This should match the base package used for the mod sources.
# See https://maven.apache.org/guides/mini/guide-naming-conventions.html
mod_group_id=com.example.extrabiomes
# The authors of the mod. This is a simple text string that is used for display purposes in the mod list.
mod_authors=Win Epic Fin
# The description of the mod. This is a simple multiline text string that is used for display purposes in the mod list.
mod_description=ExtraBiomes adds 26 new biomes to the overworld, along with the mobs, blocks, and structures to fill them - from burnt forests and jellyfish-filled oceans to a floating Sky City above the mountains.
terrablender_version=3.3.0.12
# Modrinth file/version ID for the TerraBlender-forge upload matching terrablender_version above
# (https://modrinth.com/mod/terrablender/version/terrablender_version) - see common/build.gradle.
terrablender_modrinth_id=jbRHa5Hy
# GeckoLib 1.20.4's forge, fabric, and neoforge artifacts are all at 4.4.4 (confirmed via
# api.modrinth.com/v2/project/geckolib/version - all 3 loaders published together 2024-03-17).
geckolib_version=4.4.4

## Multi-loader (Architectury) properties

# Platforms the common module targets. Forge is out of scope for this release (Fabric + NeoForge
# only) - not removed from the repo, just excluded from this branch's build/CI/publish (see
# settings.gradle and .github/workflows/*.yml).
enabled_platforms=fabric,neoforge

archives_base_name=ExtraBiomes
maven_group=net.winepicfin.extrabiomes

# Pinned to the exact resolved build rather than a floating "-SNAPSHOT" channel - a floating
# tag resolves to whatever the newest snapshot happens to be at build time, which makes the
# build non-reproducible across machines/CI (each cache could pick up a different actual
# version).
#
# Bumped to the 1.17.x/3.5.x line (from 1.11.458/3.4.164) specifically to add NeoForge support
# (Milestone B) - the 1.11/3.4 line's architectury { neoForge() } + dependencies { neoForge
# "..." } fails at Gradle configuration time ("Could not find method neoForge() for arguments
# [...]") even in the most minimal possible neoforge/build.gradle, reproduced identically on
# both 1.11.458 and the latest 1.14.x patch (1.14.476) while keeping plugin 3.4.164 - a genuine
# gap in that plugin line's NeoForge bridging, not a config mistake or a loom-only version
# issue. This 1.17.x/3.5.x line needs a newer Gradle than the wrapper was previously pinned to
# (8.14) - see gradle/wrapper/gradle-wrapper.properties, now bumped to 9.7.1 alongside this.
architectury_loom_version=1.17.491
architectury_plugin_version=3.5.169
# 10.1.20 was 1.20.2's Architectury API line; 11.1.17 is the newest line published against
# 1.20.4 (confirmed via api.modrinth.com/v2/project/architectury-api/version, all 3 loaders).
# Architectury Loom 1.17.491/plugin 3.5.169 (pinned above) already covers Forge/NeoForge up to
# the 1.21.x/26.x line, well past 1.20.4, so no further Loom/Gradle-wrapper bump is needed here.
architectury_api_version=11.1.17
fabric_loader_version=0.19.5
# Confirmed via maven.fabricmc.net - 0.19.5 is still the newest fabric-loader release (loader
# versions aren't per-Minecraft-version like fabric_api_version below).
fabric_api_version=0.97.3+1.20.4
# GeckoLib 1.20.4's fabric artifact is 4.4.4, same as forge's geckolib_version above.
geckolib_fabric_version=4.4.4
# Modrinth file id for TerraBlender-fabric 3.3.0.12 for 1.20.4 (see terrablender_modrinth_id
# above for the -forge counterpart; kept on the same TerraBlender version across loaders).
terrablender_fabric_modrinth_id=xezfbkog
# Modrinth file id for TerraBlender-neoforge 3.3.0.12 for 1.20.4 (see terrablender_modrinth_id
# above for the -forge counterpart; kept on the same TerraBlender version across loaders).
terrablender_neoforge_modrinth_id=IPdVGfJl

## NeoForge properties (Milestone B - 1.20.2 was NeoForge's first supported Minecraft version)

# The NeoForge version must agree with the Minecraft version to get a valid artifact. Confirmed
# via maven.neoforged.net/api/maven/versions/releases/net/neoforged/neoforge - 20.4.251 is the
# latest stable (non-beta) build of the 20.4.x line.
neoforge_version=20.4.251
# The NeoForge version range for mod-metadata dependency gating - matches the "neo_version_range"
# convention used by NeoForge's own MDK template (github.com/neoforged/MDK): major.minor only,
# not the full patch version.
neoforge_version_range=[20.4,)
# NeoForge's own small "FML loader" version (distinct from the neoforge_version above - this is
# the loader/API contract version, not the mod jar version), per the same MDK reference.
neoforge_loader_version_range=[1,)
