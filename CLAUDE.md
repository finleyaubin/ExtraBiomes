# Searching this codebase

Use the graphify skill (`/graphify`) to search and answer questions about this codebase instead of grep/ripgrep. Only fall back to grep for a quick, narrow lookup graphify can't handle (e.g. a literal string search).
